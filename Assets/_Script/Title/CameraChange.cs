using UnityEngine;
using UnityEngine.UI; // UIコンポーネントの参照

public class CameraChange : MonoBehaviour
{
    [SerializeField] private Slider _BGMSlider; // BGMのスライダー
    [SerializeField] private Slider _SESlider; // SEのスライダー
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
